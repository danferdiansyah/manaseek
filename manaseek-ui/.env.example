# Google OAuth Web client id. Public by design: it ships in the browser bundle.
# Create it at https://console.cloud.google.com/apis/credentials and register
# every origin the app runs on under Authorised JavaScript origins.
VITE_GOOGLE_CLIENT_ID=
